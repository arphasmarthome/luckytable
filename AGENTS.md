# Codex project instructions

Before doing project work, read and follow `PROJECT_CONTEXT.md` in the repository root.
Treat it as the shared source of truth for project architecture, conventions, workflow, and
current constraints.

Keep this file limited to Codex-specific bootstrapping. Put guidance that should also apply to
Claude Code in `PROJECT_CONTEXT.md` instead.

When a change makes shared context inaccurate, update `PROJECT_CONTEXT.md` in the same change.
Do not record secrets, credentials, or ephemeral chat details there.
